import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'web.app.dlawpowerlifting',
  appName: 'powerlifting-toolbox',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
